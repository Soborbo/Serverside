# Handover — 2026-08-17 (élesítési nap, #59 / #60 kint, #61 vár)

Ez a nap négy PR-t vitt élesbe vagy készített elő. A következő session ezzel a
dokumentummal tud folytatni.

---

## 1. Amit MINDENKÉPP tudni kell a környezetről

### A MERGE = A DEPLOY

A repón **Cloudflare Workers Builds git-integráció** fut: a `main`-be merge után
~20 másodperccel a production már a merge-commitot szolgálja ki. **Nincs külön
`wrangler deploy` lépés.** Következmények:

1. Minden ellenőrzés a **merge ELÉ** kerül (`tsc --noEmit`, `npm test`, diff).
2. **Rollback = `git revert <merge-commit>` + push**, NEM `wrangler rollback`.
   Az utóbbi a Workert visszateszi, a `main`-t nem — a production a `main` mögé
   kerül, és a másnapi drift-őr álriasztást ad pont incidens közben.
3. Reggel merge-elj: a megfigyelési ablak a merge másodpercében indul.

Ellenőrzés: `curl -s https://tracking.soborbo.co.uk/api/event/version`

### Mit TUD és mit NEM TUD ez a (távoli sandbox) környezet

| | |
|---|---|
| D1 lekérdezés (Cloudflare MCP) | ✅ működik — `8c7774d1-2eea-40ba-b99c-92e73055460f` |
| GitHub MCP (PR olvasás/merge/komment) | ✅ működik |
| **KV írás/olvasás** | ❌ **NEM** — nincs `CLOUDFLARE_API_TOKEN`, nincs tárolt wrangler OAuth (`~/.config/.wrangler/config/` nem is létezik), és a Cloudflare MCP-nek csak namespace-szintű KV-eszköze van, kulcs-szintű nincs. Emiatt a `scripts/fetch-kv-configs.mjs` sem fut (az `npx wrangler`-t hív). |
| `wrangler` bármely `--remote` parancs | ❌ ugyanezért |
| Playwright **WebKit** | ✅ működik — `playwright install webkit` **+ `install-deps webkit`** (ez utóbbi kellett: gtk4, graphene, gstreamer-bad). WebKit 26.5. |
| Playwright **Chromium** | ✅ localhoston; ❌ **kifelé** — `ERR_CONNECTION_RESET` minden élő site-on (a sandbox egress-proxyja bontja az alagutazott TLS-t). A WebKit ezzel szemben KIJUT. |
| Egress IP | 🇺🇸 `160.79.106.128`, Columbus OH — a compliance-mérés geo-kaviátja ebből ered |

---

## 2. Mi ment ki ma

| # | Mi | Merge-commit | Deploy |
|---|---|---|---|
| #62 | compliance harness + első flotta-baseline | `3b3c830` | 08:35 előtt (futásidő-semleges) |
| — | compliance baseline #2 (WebKit, relay nélkül) + riport-fejléc figyelmeztetések | `8104f49` | futásidő-semleges |
| #59 | kód-audit (guardok, álló monitor, halott kód) | `5ece5f4` | **2026-08-17T08:35:10Z** |
| #60 | Fázis D műszerezés | `bd150125` | **2026-08-17T11:48:55Z** |

Migrációk: a **0004** és **0005** már alkalmazva van az éles D1-en, és ma reggel
be lett könyvelve a `d1_migrations`-ba (id=4, id=5). **Pending migráció nulla.**
Kézzel egyiket sem kell futtatni.

### Állapot a T+2h ellenőrzéskor (14:0xZ)

- meta accepted ma **33** (08-16: 61 teljes nap) — napszaknak megfelelő ütem
- **hibakód 24 órán belül: nulla** (a várt `TRK-400-021` sem jött)
- a négy hirdetési láb skipjei egyeznek (10 · 10 · 10 · 10)
- a Fázis D **ír**: `source_used` és `source_consistent` kitöltve
- `skip_reason`: a deploy óta nincs `skipped` sor — nem hiba, még nem volt mit mérni

---

## 3. NYITOTT PR: #61 (PECR storage-gating)

**Készen áll, de MA NEM MEHET.** Egyedül kell mennie, mert ha elromlik, nem hibaüzenetet
kapsz: a konverziók tovább mennek, a ledger zöld, csak a `gclid` hiányzik belőlük,
és a Google Ads attribúció hetek alatt csendben elsorvad.

Ami már kész:
- rebase-elve a `main`-re (`--onto origin/main cb4156e`), head: `732f793`
- a PR base-e átállítva `claude/phase-d-…`-ról **`main`**-re
- `tsc` tiszta, **691/691** vitest zöld
- **Playwright WebKit 8/8 ÉS Chromium 8/8** — ez volt a hiányzó elfogadási kritérium
- a négy #61-invariáns ellenőrizve: `getFbcCookie()` a payloadban (nem `getFbc()`),
  `src/lib/consent.ts` érintetlen, `NULL ≠ 0` megmaradt, `sb_tracking` nincs kettéválasztva

Ami **hiányzik** a merge előtt:
> **Tag Assistant élesben**: `olcsokontenerhaz.hu`, `?gclid=TESZT123`, marketing
> elfogadva, tesztűrlap → a ledgerben megérkezik-e a `gclid`.
> Ezt gép nem helyettesíti; a compliance-harness szándékosan nem küld be űrlapot.

---

## 4. Push-olt, MEG NEM MERGE-ELT ágak

| ág | commit | mi |
|---|---|---|
| `fix/pecr-storage-gating` | `732f793` | a #61 (lásd fent) |
| `chore/kv-recon-setup-script` | `7fce11f` | `scripts/apply-kv-recon-config.sh` — a KV cél-állapot felírása |
| `docs/fazis-d-donteskapu` | `15451b7` | a döntési kapu átírt kritériuma |
| `docs/handover-2026-08-17` | ez a fájl | |

Egyik sincs merge-elve, mert **minden main-merge deploy**.

---

## 5. A FELHASZNÁLÓ TEENDŐI — időrendben, prioritással

### (a) `scripts/apply-kv-recon-config.sh` — a holnapi 08:15Z-i recon ELŐTT

```bash
git fetch origin chore/kv-recon-setup-script
git checkout origin/chore/kv-recon-setup-script -- scripts/apply-kv-recon-config.sh .gitignore
git checkout main   # a szkript ellenőrzi, hogy a main-en állsz
./scripts/apply-kv-recon-config.sh
```

Read-only előellenőrzés → megerősítés → írás → manifest → összevetés. Kiírja a
`country_code`-ot (az `'EU'` kérdés itt dől el), a `meta` blokk meglétét és az
apex/www egyezést. Titok nem kerül sem fájlba, sem argv-be.

Amit felír: `expected_platforms` (painless/beautyflow/lomtalan) + hat
`recon.ga4_property_id`. **A `ga4_property_id` STRING**, idézőjel nélkül a
GA4-leg csendben elszáll.

Az `expected_platforms` **aktiválja** a védőláncot. Ma ez biztonságos: az elmúlt
14 nap mind a 47 Meta-skipje a három site-on `ad_allowed=0` (consent-tiltás),
egy sem `not_configured` — tehát az írás nem gyárt azonnali CRITICAL-t.

### (b) Re-consent — a beautyflow-val kezdve

A GA4 recon-leg `analytics.readonly` scope-ot igényel; e nélkül 403-mal skippel
még akkor is, ha a `recon.ga4_property_id` fel van írva. A beautyflow OAuth-javítás
egy művelettel javítja a gads-szállítást ÉS élesíti a GA4 recon-leget.

**(a) és (b) EGYÜTT kell**: az egyik a property ID-t adja, a másik a jogosultságot.

### (c) 🔴 ÚJ, MA DERÜLT KI: a kliens-lib 6.1.0 kitelepítése a site-okra

**A Fázis D órája elindult, de az egyik lába nincs a földön.**

A `computeSourceConsistency` egyetlen nem-NULL forrásnál szerkezetileg **1**-et ad:

```ts
const values = sources.map((s) => s[key]).filter((v) => v !== null);
if (values.some((v) => v !== values[0])) consistent = false;  // 1 elemnél sosem igaz
```

A mai receiptekben a cookie- és api-forrás NULL (a `consent_sources` blokkot a
**6.1.0**-s kliens-lib küldi, az még nincs kint), tehát csak a szerver-süti volt
jelen → `source_consistent = 1` **nem egyetértésből, hanem forráshiányból**.

Következmény: az átírt kapu-kritérium (`source_consistent=0` arány) **ugyanabba a
csapdába esik**, mint a nyugdíjazott `≥10 GRANTED-skip`: nincs alanya, amíg a
6.1.0 nem megy ki. A NULL-mintázat rész működik és már most ad adatot; a
konzisztencia-rész vak.

Javasolt pontosítás (nem eldöntött):

```
a `source_consistent=0` arány CSAK azokra a receiptekre értelmezhető, ahol
legalább KÉT nem-NULL forrás van
  → az 500-as küszöb ERRE a részhalmazra vonatkozzon
  → és az óra a 6.1.0 kitelepítésével induljon, ne a #60 deployjával
```

### (d) #61 Tag Assistant ellenőrzés, majd merge — külön napon

---

## 6. Ütemezett önellenőrzés

**2026-08-18 08:45Z** — KAPU 1+2 együtt (trigger él ebben a session-ben). Nézi:
volumen, `skip_reason` az új sorokon, consent-forrás oszlopok, TRK-950-011,
SITE_CONFIG_DRIFT, hibakódok. Mindkét deploy-időbélyeggel, hogy a jel
szétválasztható legyen. A recon `15 8 * * *` (08:15Z), a digest `0 8 * * *`.

---

## 7. Megállapítások, amiket a következő kör ne veszítsen el

**A ledger `not_expected` sorai el vannak nyomva** (F4-2, `conversion.ts:1044`
`isNotExpectedSkip`) — ez NEM a #59 hozta, a #59 csak az AE-metrikát hozta
szinkronba vele. Amit a `deliveries`-ben scaffold-láb sorként látsz, az
consent-tiltás vagy meghatározatlan skip-okú ág (LinkedIn „nincs matchelhető
azonosító", MsAds scaffold-only), nem konfigurációs zaj.

**A jövőbeli riasztható invariáns** (a cross-check következő körébe, most NEM
építve):
```
tiktok_skipped == linkedin_skipped == msads_skipped            → különben anomália
meta_skipped(ad_allowed=0) == tiktok_skipped(ad_allowed=0)     → a consent-keret zárul
```
A státusz-alapú változat (`meta_skipped + meta_rejected == a három láb`) **NEM
igaz** — 30 napon 172+43=215 vs 210. A keret az `ad_allowed` mentén zárul, nem a
státusz mentén.

**A compliance-harness két dolgot tud magáról**, és mindkettő a `report.md`
fejlécében van:
1. **Optimista irányba téved** — kétszer adott hamis PASS-t (első félen proxyzott
   Google-mérés; ITP által eldobott süti), és mindkettőt ember fogta meg, nem a teszt.
2. **Nem determinisztikus** — ugyanaz a flotta három futáson 55/51/53 FAIL, a
   `nemesventilatorhaz`+`szelloztetes` oldalakon koncentrálva. Az oszlop-összesítés
   stabil; EGYETLEN site FAIL-számára nem szabad következtetést építeni. **Ez a
   következő kör legfontosabb javítása** (`tests/compliance/README.md`).

**A `nemesventilatorhaz.hu` döntés ELŐTT olvassa a `_fbp`/`_fbc` kulcsot a
localStorage-ból** — az egyetlen storage-olvasás találat a flottán, és ez a #61
élő indoklása.

**A Fázis D „megmagyarázatlan GRANTED-skip" halmaza lezárt ügy**: 60 napon 11
esemény, mind 2026-07-15 és 07-21 között (a lomtalan-incidens ablaka), azóta 27
nap nulla. Részletek: `docs/2026-08-17-fazis-d-donteskapu.md`.

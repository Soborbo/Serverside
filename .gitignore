node_modules/
dist/
.wrangler/
.env
.env.local
.dev.vars
*.log
.DS_Store

# Go-live token material — plaintext per-site tokens + rotated admin token.
# NEVER commit: the KV side stores only the SHA-256.
secrets/
prod-audit/

# Compliance harness: az önteszt kimenete tranziens (a dátumozott flotta-baseline
# viszont MEGY a repóba — az a mérés lényege).
tests/compliance/selftest/last-run/

# A KV-ből újragenerált manifest (scripts/apply-kv-recon-config.sh). Titok-mentes
# (a generátor sha256-tal helyettesíti), de MUNKAFÁJL — a repóba a src/site-manifest.json megy.
site-manifest.generated.json

# §15 — a futásidejű error-code emisszió-napló (a vitest setup-hook írja,
# a check-error-code-emission.mjs olvassa). Munkatermék, nem forrás.
.error-code-emission/

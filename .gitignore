node_modules/
dist/
.wrangler/
.env
.env.local
.dev.vars
*.log
.DS_Store

# Go-live token material — plaintext per-site tokens + rotated admin token.
# NEVER commit: the KV side stores only the SHA-256.
secrets/
prod-audit/

# Compliance harness: az önteszt kimenete tranziens (a dátumozott flotta-baseline
# viszont MEGY a repóba — az a mérés lényege).
tests/compliance/selftest/last-run/
